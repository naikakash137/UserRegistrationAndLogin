# SpringBootRestApplication
simple spring boot application having rest controller in it. 

simple springboot application built on maven architecture having profiles(dev, prod) with controller and service layer added to it.
